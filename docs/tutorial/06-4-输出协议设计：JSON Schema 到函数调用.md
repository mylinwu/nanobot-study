# 06-4｜输出协议设计：JSON Schema 到函数调用

## 1. 协议目标

让下游“稳定消费”而不是“猜测解析”。

## 2. 两条路线

- JSON Schema：通用性高。
- Function Calling：强约束更稳定。

## 3. 设计要点

- 必填字段最少化但关键字段必须有。
- 枚举值封闭。
- 错误对象统一结构。

## 4. 练习

为“调研报告输出”定义函数签名，包含结论、证据、风险、计划。
