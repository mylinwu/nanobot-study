# 04-3｜工具安全与权限：最小授权到审计闭环

## 1. 风险背景

工具层一旦可执行外部动作（DB写入、API调用），就必须有权限边界。

## 2. 最小授权原则

- 读写分离 token。
- 高风险工具默认禁用。
- 按任务类型开放白名单。

## 3. 审计闭环

每次调用都要记录：

- 调用者
- 工具
- 参数摘要
- 结果摘要
- 责任链（trace_id）

## 4. 练习

为 `sql_write` 设计审批机制：哪些条件下必须人工确认？
